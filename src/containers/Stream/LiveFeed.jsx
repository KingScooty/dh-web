var React = require('react');

var LiveFeed = React.createClass({

  render: function () {
    // var Posts = this.getPosts();

    return (
      <h1>[LIVE!] FEED!</h1>
    );
  }
});

module.exports = LiveFeed;
